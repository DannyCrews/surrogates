<header>
<h1>Tasks for <%= link_to(@surrogate.name, @surrogate) %></h1>
</header>

<% @tasks.each do |task| %>
  <article>
    <p>
      <%= task.description %>
    </p>
    <p>
      <%= task.status %>
    </p>
  </article>

  
<% end %>
